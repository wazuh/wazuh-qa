- hosts: Manager*
  become: true
  tasks:
    - name: Install the Wazuh manager
      command: "yum -y install wazuh-manager"

- hosts: Agent*
  become: true
  tasks:
    - name: Install the Wazuh agent
      command: "yum -y install wazuh-agent"