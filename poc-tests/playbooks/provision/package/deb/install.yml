- hosts: Manager*
  become: true
  tasks:
    - name: Install the Wazuh manager
      command: "apt-get install wazuh-manager"

- hosts: Agent*
  become: true
  tasks:
    - name: Install the Wazuh agent
      command: "apt-get install wazuh-agent"