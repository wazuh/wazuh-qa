- hosts: all
  become: true
  vars:
    path: '/tmp'
    test_directory: "tests"
    test_file: "test.py"


  tasks:
    - name: Create test directory
      file:
        path: "{{ path }}/{{ test_directory }}"
        state: directory

    - name: Copy files to endpoints
      copy:
        src: "{{ item }}"
        dest: "{{ path }}/{{ test_directory }}"
      with_items: "../../{{ test_directory }}/"




