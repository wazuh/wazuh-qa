all:
  hosts:
    Agent1:
      ansible_host: 172.16.1.2
      ansible_port: 22
      ansible_user: 'vagrant'
      ansible_ssh_private_key_file: '/root/.ssh/id_rsa'
      distribution: debian
      manager: 172.16.1.3
      install:
        - type: package
          component: wazuh-agent
        - component: curl
        - component: nano

    Manager:
      ansible_host: 172.16.1.3
      ansible_port: 22
      ansible_user: 'vagrant'
      ansible_ssh_private_key_file: '/root/.ssh/id_rsa'
      distribution: debian
      install:
        - type: aio
          componente: wazuh-server

  vars:
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
