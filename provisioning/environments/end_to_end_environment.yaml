centos-manager:
  os: amazonlinux
  roles: [aio]

ubuntu-agent:
  os: ubuntu
  roles: [agent]
  manager: centos-manager

centos-agent:
  os: centos
  roles: [agent]
  manager: centos-manager

windows-agent:
  os: windows
  roles: [agent]
  manager: centos-manager
