- hosts: all
  become: true
  tasks:
    - name: Test {{ test }} for {{ component }}
      command: "pytest test_{{component}}/test_{{ test }}.py  -vs --wazuh_version={{ wazuh_version }} --component={{ component }} --dependency_ip={{ dependency_ip }}"
      args:
        chdir: "{{ working_dir }}"