- vagrant:
    key:
      type: dynamic
      name: ${uuid}
    instance-name:
      format: ${uuid}
    tags:
      type: qa

- aws:
  security-group: [sg-0877d224f9c5b2708]
  key:
    type: dynamic
    name: ${uuid}
  tags:
    type: qa
    name: ddt1-{name}
