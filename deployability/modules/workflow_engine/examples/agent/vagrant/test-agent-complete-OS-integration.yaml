version: 0.1
description: This workflow is used to test agents' deployment for DDT1 PoC
variables:
  agent-os:
    - windows-server-2022-amd64
    - macos-sonoma-14.0-arm64
    - linux-ubuntu-22.04-amd64
  manager-os: linux-ubuntu-22.04-amd64
  infra-provider: vagrant
  working-dir: /tmp/dtt1-poc

tasks:
  # Unique manager allocate task
  - task: "allocate-manager-{manager-os}"
    description: "Allocate resources for the manager."
    do:
      this: process
      with:
        path: python3
        args:
          - modules/allocation/main.py
          - action: create
          - provider: "aws"
          - size: large
          - composite-name: "{manager-os}"
          - inventory-output: "{working-dir}/manager-{manager-os}/inventory.yaml"
          - track-output: "{working-dir}/manager-{manager-os}/track.yaml"
          - label-termination-date: "1d"
          - label-team: "qa"
    on-error: "abort-all"
    cleanup:
      this: process
      with:
        path: python3
        args:
          - modules/allocation/main.py
          - action: delete
          - track-output: "{working-dir}/manager-{manager-os}/track.yaml"

  # Unique agent allocate task
  - task: "allocate-agent-windows-server-2022-amd64"
    description: "Allocate resources for the agent windows."
    do:
      this: process
      with:
        path: python3
        args:
          - modules/allocation/main.py
          - action: create
          - provider: "aws"
          - size: small
          - composite-name: "windows-server-2022-amd64"
          - inventory-output: "{working-dir}/agent-windows-server-2022-amd64/inventory.yaml"
          - track-output: "{working-dir}/agent-windows-server-2022-amd64/track.yaml"
          - label-termination-date: "1d"
          - label-team: "qa"
    on-error: "abort-all"
    cleanup:
      this: process
      with:
        path: python3
        args:
          - modules/allocation/main.py
          - action: delete
          - track-output: "{working-dir}/agent-windows-server-2022-amd64/track.yaml"
    depends-on:
      - "provision-manager-{manager-os}"

  # Unique agent allocate task
  - task: "allocate-agent-macos-sonoma-14.0-arm64"
    description: "Allocate resources for the agent sonoma."
    do:
      this: process
      with:
        path: python3
        args:
          - modules/allocation/main.py
          - action: create
          - provider: "vagrant"
          - size: small
          - composite-name: "macos-sonoma-14.0-arm64"
          - inventory-output: "{working-dir}/agent-macos-sonoma-14.0-arm64/inventory.yaml"
          - track-output: "{working-dir}/agent-macos-sonoma-14.0-arm64/track.yaml"
          - label-termination-date: "1d"
          - label-team: "qa"
    on-error: "abort-all"
    cleanup:
      this: process
      with:
        path: python3
        args:
          - modules/allocation/main.py
          - action: delete
          - track-output: "{working-dir}/agent-macos-sonoma-14.0-arm64/track.yaml"
    depends-on:
      - "provision-manager-{manager-os}"

  # Unique agent allocate task
  - task: "allocate-agent-linux-ubuntu-22.04-amd64"
    description: "Allocate resources for the agent ubuntu."
    do:
      this: process
      with:
        path: python3
        args:
          - modules/allocation/main.py
          - action: create
          - provider: "vagrant"
          - size: small
          - composite-name: "linux-ubuntu-22.04-amd64"
          - inventory-output: "{working-dir}/agent-linux-ubuntu-22.04-amd64/inventory.yaml"
          - track-output: "{working-dir}/agent-linux-ubuntu-22.04-amd64/track.yaml"
          - label-termination-date: "1d"
          - label-team: "qa"
    on-error: "abort-all"
    cleanup:
      this: process
      with:
        path: python3
        args:
          - modules/allocation/main.py
          - action: delete
          - track-output: "{working-dir}/agent-linux-ubuntu-22.04-amd64/track.yaml"
    depends-on:
      - "provision-manager-{manager-os}"

  # Unique manager provision task
  - task: "provision-manager-{manager-os}"
    description: "Provision the manager."
    do:
      this: process
      with:
        path: python3
        args:
          - modules/provision/main.py
          - inventory: "{working-dir}/manager-{manager-os}/inventory.yaml"
          - install:
            - component: wazuh-manager
              type: assistant
              version: 4.7.3
              live: True
    depends-on:
      - "allocate-manager-{manager-os}"
    on-error: "abort-all"


  # Generic agent test task
  - task: "run-agent-{agent}-tests"
    description: "Run tests install for the agent {agent}."
    do:
      this: process
      with:
        path: python3
        args:
          - modules/testing/main.py
          - targets:
            - wazuh-1: "{working-dir}/manager-{manager-os}/inventory.yaml"
            - agent: "{working-dir}/agent-{agent}/inventory.yaml"
          - tests: "install,registration,basic_info,connection,restart,stop,uninstall"
          - component: "agent"
          - wazuh-version: "4.7.3"
          - wazuh-revision: "40714"
          - live: "True"
    foreach:
      - variable: agent-os
        as: agent
    depends-on:
      - "allocate-agent-windows-server-2022-amd64"
      - "allocate-agent-linux-ubuntu-22.04-amd64"
      - "allocate-agent-macos-sonoma-14.0-arm64"
