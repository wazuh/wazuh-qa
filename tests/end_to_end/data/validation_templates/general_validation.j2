- name: General validation phase
  hosts: {{ target_hosts }}
  gather_facts: false
  roles:
    - role: host_checker
      vars:
        os: "{% raw %}{{ hostvars[inventory_hostname]['os_name'] }}{% endraw %}"
        supported_distros: {{ distros }}
