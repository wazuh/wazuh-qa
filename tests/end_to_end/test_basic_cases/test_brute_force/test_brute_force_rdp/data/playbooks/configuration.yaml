- name: Configure local environment
  hosts:  centos-manager
  become: true
  tasks:
    # Install hydra to attempt the RDP brute force attack
    - name: Install hydra
      ansible.builtin.yum:
        name: hydra
        state: present
