---
  - name: "Delete FIM test files at Wazuh Managers"
    script: "modify_files.py -i {{ input_list }} -o {{ output_list }}"
    args:
      executable: "python3"
    when:
      - inventory_hostname in groups['linuxagents'] or inventory_hostname in groups['windowsagents']