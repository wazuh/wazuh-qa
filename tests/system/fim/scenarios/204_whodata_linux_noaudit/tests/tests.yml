---
- name: Check whether ossec.log contains switching to realtime message.
  command: grep "Who-data engine could not start. Switching who-data to real-time." {{ linux_ossec_folder_path }}/logs/ossec.log
  changed_when: false
  when:
    - inventory_hostname in groups['linuxagents']
