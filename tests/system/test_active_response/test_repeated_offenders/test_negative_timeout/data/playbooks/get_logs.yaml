- name: Copy log file to a tmp folder
  hosts: manager
  become: true
  tasks:

    - name: Get ossec log
      include_role:
        name: manage_logs
        tasks_from: get_log_file.yaml
      vars:
        dest_path: test_negative_timeout/
        source_path: /var/ossec/logs/ossec.log
