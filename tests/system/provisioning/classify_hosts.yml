---
    - hosts: localhost
      vars_files:
      - ./vars/aws.yml
      - ./vars/credentials.yml
      - ./vars/wazuh_manager.yml
      - ./vars/elastic_nginx.yml
      tasks:
        - import_tasks: ../provisioning/roles/aws/tasks/classify_hosts.yml