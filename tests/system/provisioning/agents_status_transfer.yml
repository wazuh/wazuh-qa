---
  - hosts: all
    tasks:
      - name: Get agents status from manager node
        become: true
        synchronize:
          mode: pull
          src: /opt/fim_testing
          dest: ./agents_status
        when:
          - inventory_hostname in groups['masters']