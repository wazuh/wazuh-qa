all:
  hosts:
    wazuh-manager:
      ansible_connection: docker
      ansible_python_interpreter: python
    wazuh-agent1:
      ansible_connection: docker
      ansible_python_interpreter: python
managers:
  hosts:
    wazuh-manager:
      ansible_connection: docker
      ansible_python_interpreter: python
agents:
  hosts:
    wazuh-agent1:
      ansible_connection: docker
      ansible_python_interpreter: python
