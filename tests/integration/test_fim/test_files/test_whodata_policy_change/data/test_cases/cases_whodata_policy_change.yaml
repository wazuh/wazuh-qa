- name: audit_policy_change_event_checker
  description: When a file is monitored with report_changes, the diff file and changes are reported (Scheduled mode)
  configuration_parameters:
    AUDIT_INTERVAL: 5
  metadata:
    check_event: false
    disabling_file: policy_disable.csv
    enabling_file: policy_enable.csv

- name: audit_policy_change_event_4719
  description: When a file is monitored with report_changes, the diff file and changes are reported (Scheduled mode)
  configuration_parameters:
    AUDIT_INTERVAL: 5
  metadata:
    check_event: true
    disabling_file: policy_no_success.csv
    enabling_file: policy_enable.csv