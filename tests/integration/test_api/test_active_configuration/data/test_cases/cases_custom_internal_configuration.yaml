- name: remoted_enpoint
  description: remoted internal configuration endpoint
  configuration_parameters:
    ENDPOINT: request
  metadata:
    endpoint: request

- name: analysisd_endpoint
  description: analysisd internal configuration endpoint
  configuration_parameters:
    ENDPOINT: analysis
  metadata:
    endpoint: analysis

- name: wazuh-db_endpoint
  description: wazuh-db internal configuration endpoint
  configuration_parameters:
    ENDPOINT: wazuh-db
  metadata:
    endpoint: wazuh-db
