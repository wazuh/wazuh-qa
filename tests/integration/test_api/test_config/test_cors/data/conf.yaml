---
- tags:
  - cors
  configuration:
    cors:
      enabled: yes
      source_route: "https://test_url.com"
      expose_headers:
        - "Test-expose"
        - "another-expose"
      allow_headers:
        - "Content-type"
      allow_credentials: true
