- name: Verify filtering using an existent kvdb
  description: Verify that listing an existent kvdb works as expected.
  configuration_parameters:
    cfg: TODO
  metadata:
    command: kvdb
    subcommand: list
    options:
      -n: kvdb_test
    kvdb_names:
      - kvdb_test

- name: Verify filtering using an empty kvdb
  description: Verify that listing an empty kvdb works as expected.
  configuration_parameters:
    cfg: TODO
  metadata:
    command: kvdb
    subcommand: list
    options:
      -n: kvdb_empty
    kvdb_names:
      - kvdb_empty

- name: Verify listing a non-existent kvdb
  description: Verify that listing a non-existing kvdb works as expected.
  configuration_parameters:
    cfg: TODO
  metadata:
    command: kvdb
    subcommand: list
    options:
      -n: kvdb_test
    kvdb_names:
      - kvdb_empty

- name: Verify listing all the existent kvdbs
  description: Verify that all the created kvdbs can be listed.
  configuration_parameters:
    cfg: TODO
  metadata:
    command: kvdb
    subcommand: list
    kvdb_names:
      - kvdb_empty
      - kvdb_test

- name: Verify listing existent kvdbs using db name
  description: Verify that list subcommand can filter kvdb searches.
  configuration_parameters:
    cfg: TODO
  metadata:
    command: kvdb
    subcommand: list
    options:
      -n: kvdb_test
    kvdb_names:
      - kvdb_empty
      - kvdb_test

- name: Verify listing existent kvdbs using common substring
  description: Verify that list subcommand can filter kvdb searches.
  configuration_parameters:
    cfg: TODO
  metadata:
    command: kvdb
    subcommand: list
    options:
      -n: kvdb_
    kvdb_names:
      - kvdb_empty
      - kvdb_test
