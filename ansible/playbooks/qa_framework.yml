- hosts: qa_framework
  roles:
    - ../roles/qa_framework
  vars:
    qa_hosts:
      - port: 1514
        protocol: tcp
        api_port: 55000
        api_proto: 'http'
        api_user: ansible
        max_retries: 5
        retry_interval: 5
